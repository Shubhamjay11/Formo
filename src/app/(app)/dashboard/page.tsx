/**
 * Placeholder dashboard. The authenticated (app) layout with session + org
 * enforcement is built in docs/features/001-auth.md and 002-workspaces.md.
 */
export default function DashboardPage() {
  return <div className="p-8 text-muted-foreground">Dashboard — build via feature specs.</div>;
}
