/**
 * Drizzle schema. Conventions: see .cursor/rules/30-database.mdc.
 * Tables are added per feature spec (001-auth adds users/sessions,
 * 002-organizations adds organizations/memberships/invitations, ...).
 */
export {};
